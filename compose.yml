services:
  postgres:
    image: postgres:18-alpine
    restart: always
    networks:
      - library-network
    ports:
      - 5432:5432
    volumes:
      - postgres-data:/var/lib/postgresql/data

networks:
  library-network:

volumes:
  postgres-data:
